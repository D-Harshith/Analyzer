services:
  - type: web
    name: ai-analyzer-app
    env: python
    plan: free
    buildCommand: |
      pip install -r requirements.txt
      playwright install --with-deps
    startCommand: streamlit run app.py
